{{- if .Values.secret.create }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.secret.name }}
type: Opaque
stringData:
  DB_URL: {{ required "secret.DB_URL is required" .Values.secret.DB_URL | quote }}
{{- end }}
