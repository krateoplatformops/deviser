apiVersion: v1
kind: Service
metadata:
  name: {{ include "deviser.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "deviser.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "deviser.selectorLabels" . | nindent 4 }}
